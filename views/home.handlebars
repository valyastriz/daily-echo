{{!-- homepage template --}}
<main class="main-content">
    <aside class="sidebar">
        {{#if logged_in}}
            <button id="new-entry-btn" class="new-entry-btn">New Entry</button>
            <ul class="entry-list">
                {{#each entries}}
                <li class="entry-item">
                    <button class="view-entry-btn" data-id="{{this.id}}">{{this.title}}</button>
                </li>
                {{/each}}
            </ul>
        {{else}}
            <form id="login-form">
                <label for="username">Username:</label>
                <input type="text" id="name" name="name" required>
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
                <button type="submit">Log In</button>
                <a href="/signup">Sign Up</a>
            </form>
        {{/if}}
    </aside>

    <section id="entry-details" class="entry-details">
        {{#if logged_in}}
            <p>Select an entry to view details, or click 'New Entry' to create a new one.</p>
        {{else}}
            <h1>Welcome...</h1>
            <br>
            <p>Welcome to Daily Echo – Your Private Space for Daily Reflections</p>
            <br>
            <p>In a fast-paced world, finding a moment to reflect and capture your thoughts is essential. Daily Echo offers a serene and secure environment to document your daily experiences, emotions, and ideas. Whether it's work-related insights or personal musings, our intuitive platform allows you to effortlessly categorize entries and revisit them with ease. Dive into your past through our interactive calendar view, and let your thoughts echo across time. Start your journey of self-discovery and personal growth with Personal Diary today.</p>
        {{/if}}
    </section>

    <script src="/js/login.js"></script>
    {{#if logged_in}}
    <script src="/js/home.js"></script>
    {{/if}}
</main>
